import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'si.tomazdrnovsek.absolutist',
  appName: 'The Absolutist',
  webDir: 'dist'
};

export default config;
